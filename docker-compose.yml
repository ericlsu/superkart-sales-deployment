version: '3.8'

services:
  backend:
    build: ./backend
    container_name: backend
    ports:
      - "7860:7860"
    networks:
      - superkart-network

  frontend:
    build: ./frontend
    container_name: frontend
    ports:
      - "8501:8501"
    depends_on:
      - backend
    networks:
      - superkart-network

networks:
  superkart-network:
    driver: bridge
